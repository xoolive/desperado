[package]
name = "traffic"
description = "TPEG/TEC and RDS-TMC traffic-event decoding with GeoJSON output"
keywords = ["tpeg", "tec", "rds", "tmc", "traffic"]
readme = "readme.md"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
